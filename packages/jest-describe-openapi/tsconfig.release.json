{
  // This exists as a separate release config, so that we do not
  // include tests in our build but still gain the benefit of refactors
  // affecting test files when using typescript-aware IDEs.
  "extends": "./tsconfig.json",
  "exclude": ["**/*.spec.ts", "**/*.test.ts"]
}
